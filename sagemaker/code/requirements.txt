# Intentionally EMPTY (no runtime pip install).
#
# Why: the serving image is the DLC-fixed image (Dockerfile.dlc), which already
# has the EXACT, working stack baked in — optimum-neuron 0.4.5 (with
# NeuronFluxKontextPipeline), diffusers 0.35.2, transformers 4.48, the matching
# neuronx-cc 2.25 / torch-neuronx, plus tenacity / sentencepiece / protobuf.
#
# Listing deps here is ACTIVELY HARMFUL: SageMaker/torchserve pip-installs
# code/requirements.txt at container start, and an UNPINNED `optimum-neuron[neuronx]`
# re-resolves and DOWNGRADES the image's good optimum-neuron 0.4.5 to a version
# WITHOUT NeuronFluxKontextPipeline -> model_fn dies with
# "ImportError: cannot import name 'NeuronFluxKontextPipeline'".
# (Standalone import of the image works; it only breaks AFTER this file installs.)
#
# So: keep this empty. All serving deps come from the image. If you ever switch
# off the DLC-fixed image, pin EXACT versions here instead of leaving it open.
